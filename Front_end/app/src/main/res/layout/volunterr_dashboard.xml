<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/cute"
    android:padding="16dp">

    <!-- Small dog-cat logo, lifted up (ID unchanged) -->
    <!-- negative -> lifts up -->

    <!-- Welcome text (keeps your ID and location under the image) -->
    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"

        android:layout_centerHorizontal="true"
        android:layout_marginTop="6dp"
        android:text="Welcome, Volunteer!"
        android:textColor="#222A22"
        android:textSize="25sp"
        android:textStyle="bold" />

    <!-- Buttons container pinned to the bottom so image stays visible -->
    <LinearLayout
        android:id="@+id/button_container"
        android:layout_width="377dp"
        android:layout_height="349dp"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:orientation="vertical"
        android:paddingStart="20dp"
        android:paddingEnd="20dp">

        <Button
            android:id="@+id/admin_manage"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/rounded_edittext"
            android:backgroundTint="#66BB6A"
            android:text="View Requests"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />

        <Button
            android:id="@+id/admin_view"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/rounded_edittext"
            android:backgroundTint="#66BB6A"
            android:text="Upload Pet 🐶"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />

        <Button
            android:id="@+id/admin_set"
            android:layout_width="match_parent"
            android:layout_height="48dp"
            android:layout_marginBottom="12dp"
            android:background="@drawable/rounded_edittext"
            android:backgroundTint="#66BB6A"
            android:text="👤 Edit Profile"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textStyle="bold" />

    </LinearLayout>

</RelativeLayout>
